// import { axiosInstance } from "@/lib/axios";

// Dashboard-specific API calls go here.
// Currently, the dashboard relies heavily on Room APIs (e.g., getting a manager's rooms).
// Those room-centric API calls are located in `@/features/rooms/repository.ts`.
// When dashboard-specific metrics or aggregations APIs are added, they should be placed here.
